import ProductLandingPage from '@/app/components/ProductLandingPage';
import { DEXCONTRACTS_CONFIG } from '@/app/utils/product-pages';

export default function DexContracts() {
  return <ProductLandingPage config={DEXCONTRACTS_CONFIG} />;
}
